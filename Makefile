.PHONY: fmt lint test check validate
fmt:
	@echo "(fmt) nada a fazer — configure no projeto"
lint:
	@echo "(lint) nada a fazer — configure no projeto"
test:
	@echo "(test) nada a fazer — configure no projeto"
check validate:
	bash scripts/validate.sh
